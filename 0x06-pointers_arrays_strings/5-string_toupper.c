#include "main.h"
/**
 *string_toupper- concatenation
 *@char- character
 *Return: n
 */
char *string_toupper(char *)
{
	int i;
	int *n;

	i = 0;
	for (n[i] != '\0';)
	{
		if (n[i] >= a && n[i] <= 'z')
			n[i] = n[i] - 32;
		i++;
	}
	return (n);
}
