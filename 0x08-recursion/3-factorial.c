#include "main.h"
/**
 *factorial- prints a string followed by a new line
 *@n: The string to be printed
 *Return: If n > 0the factorial of n.
 */
int factorial(int n)
{
	int result = n;

	if (n < 0)
		return (-1);
	else if (n >= 0 && n <= 1)
		return (1);
}
