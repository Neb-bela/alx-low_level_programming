#include "main.h"
/**
 *_abs- checks if lowercase
 *@int: integer
 * Return: Always 0
 */
int _abs(int)
{
	int c;
	int d;

	if (c >= 0)
		printf("%d\n", c);
	else
	{
		d = -1 x c;
		printf("%d\n", d);
	}
	return (0);
}
